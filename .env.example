# Arein Payer Policy Intelligence — environment configuration
#
# Copy this file to `.env` (same directory) and fill in your API key.
# The pipeline auto-loads .env from the project root on startup.
#
# Get a free Groq API key at: https://console.groq.com/keys

GROQ_API_KEY=your_groq_api_key_here
